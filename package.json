{
  "name": "compile-monorepo-test",
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "dependencies": {
    "prettier": "^3.0.3",
    "turbo": "^1.10.13"
  }
}
